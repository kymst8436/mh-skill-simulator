# MHシミュレーター プロジェクトルール

## ローカライズ(必須ルール)

本アプリは7言語対応(ja / en / fr / de / es / pt-BR / ko)。**ユーザー向け文言を追加・変更する際は、例外なく以下の手順を踏むこと。**

### 1. 全対応言語への反映

- UI文言は日本語リテラルをキーとして `app/MHSimulator/MHSimulator/Localizable.xcstrings` で管理する
  - LocalizedStringKey位置(Text/Button等の直書きリテラル)はそのまま、String型の位置は `String(localized:)` で包む
  - キーの抽出・同期は `xcodebuild -exportLocalizations`(またはXcodeビルド)で行う
- 追加・変更したキーには**7言語すべての翻訳**を入れる。一部言語だけの反映は禁止
- ゲーム用語(武器種・部位・スキル分類等)はモンハン公式訳語に合わせる。迷ったら `data/source/merged/` の各言語 `names` を根拠にする
- 未翻訳が残っていないことを `python3 tools/check_localizations.py` で確認する(0件でないうちは作業完了としない)
- マスタデータ由来の文言(スキル名等)は bundled.db の言語列が正。データ側の変更は `tools/convert/convert.py` の `LANGUAGES` と仕様.md 4.1 に従う

### 2. ネイティブレビュー(サブエージェント)の実行

- 翻訳を投入したら、**対象言語ごとにネイティブレビュー役のサブエージェント(Agentツール)を並列で起動**し、以下の観点でレビューさせる:
  - ネイティブとして自然な表現か(直訳調・不自然な敬語・語順の違和感)
  - モンハン公式用語との整合(武器種名・スキル・護石=Charm/Talisman/Amuleto等)
  - フォーマット指定子(%@ / %lld / 位置指定 %1$@)の破損がないか
  - UI上の長さ(ボタン・バッジ・タブ等で極端に長くないか)
- レビュー指摘は xcstrings に反映してから完了とする。反映しない指摘は理由を残す
- レビュー完了前にコミット・PR作成をしない

### 3. レイアウト確認

- 長文になりやすい言語(de / es / fr)で、変更画面の見切れ・折り返しをシミュレータで確認する
- 固定幅 `.frame(width:)` にテキストを入れない(`minWidth` + `fixedSize` を使う)

### 補足

- OCR(護石カメラ)の言語別語彙は Core の `CharmScanProfile` に集約。日本語以外のゲームUI表記は実機スクリーンショットで検証してから変更する
- アプリのブランチ運用: mainへ直コミット禁止、フィーチャーブランチ+PR経由
