# 画面設計 — MHシミュレーター

## 1. この文書の位置づけ

- 対象プロダクト: MHシミュレーター(MH Simulator)
- 元になる仕様: `Docs/仕様.md`(2026-08-23改訂版)
- 対象スタック: iOS 17+ / SwiftUI / MVVM(`@Observable` ViewModel)/ iPhone縦固定
- コアロジック: `app/MHSimulatorCore/`(実装済み。`MasterDatabase` / `SearchEngine` / `CharmOracle` / `CharmRules` / `ResultValidator`)
- UI部品: SwiftUI標準のみ(List / NavigationStack / TabView / Stepper / TextField / ContentUnavailableView / confirmationDialog / sheet)。独自デザインシステムは持たない
- 広告: バナー広告(SDK選定は未確定事項1)。表示位置は本書で確定する

余白・フォントはSwiftUIの既定(システムフォント+Dynamic Type)に従う。この文書では**画面の構成・文言・状態・ViewModelの形だけ**を確定し、色・余白の再定義はしない。

### 命名規則(iOS版)

画面名 `SearchCondition` の場合:

| 項目 | 値 |
|---|---|
| View | `app/MHSimulator/Sources/Views/SearchConditionView.swift` |
| ViewModel | `app/MHSimulator/Sources/ViewModels/SearchConditionViewModel.swift` |
| Navigation | タブ内 `NavigationStack` の `Route.searchCondition`(enum case) |

シート表示の画面も同じ規則。ダイアログはSwiftUIの `confirmationDialog` / `alert` で実装し、独立Viewを作らない。

---

## 2. 画面一覧

| # | 画面名 | Route | 採用パターン | 目的 | タブ | MVP |
|---|---|---|---|---|---|---|
| 1 | 検索条件 | `searchCondition` | Data Entry(モバイル適用) | スキル条件を組んで検索を実行する | 検索(ルート) | ○ |
| 2 | スキル選択 | `skillPicker`(sheet) | Master Detail(一覧のみ) | 179スキルから条件スキルを素早く選ぶ | 検索 | ○ |
| 3 | 武器選択 | `weaponSelect` | Master Detail(一覧のみ) | 使う武器を選び検索に反映する | 検索 | Phase 4 |
| 4 | 検索結果 | `searchResults` | パターン外(結果リスト+逆引き) | 組める装備を確認する。0件なら次の一手を得る | 検索 | ○ |
| 5 | 装備詳細 | `equipmentDetail` | パターン外(詳細カード) | 装備一式の内訳を確認する | 検索 | ○ |
| 6 | 護石一覧 | `charmList` | Master Detail(一覧のみ) | 所持護石を管理する | 護石(ルート) | Phase 4 |
| 7 | 護石入力 | `charmEntry`(sheet) | Data Entry | 規則補助付きで護石を登録・編集する | 護石 | Phase 4 |
| 8 | 情報 | `info` | Settings App(簡易) | バージョン・データ情報・権利表記への入口 | 情報(ルート) | Phase 5 |
| 9 | 権利表記 | `credits` | パターン外(静的文書) | 著作権・データ出典・非公式表明の表示 | 情報 | Phase 5 |

タブ構成: `検索` / `護石` / `情報` の3タブ(TabView)。起動直後は `検索` タブの検索条件画面。

### ダイアログ一覧

| # | ダイアログ | 種類 | 呼び出し元 | 目的 | 文言と選択肢 |
|---|---|---|---|---|---|
| 1 | 護石削除確認 | confirmationDialog | 6. 護石一覧 | 破壊的操作の確認 | 「この護石を削除しますか?」[削除(destructive) / キャンセル] |
| 2 | 入力破棄確認 | confirmationDialog | 7. 護石入力 | 未保存変更の破棄確認 | 「入力内容を破棄しますか?」[破棄(destructive) / 続ける] |
| 3 | 重複登録警告 | alert | 7. 護石入力 | 完全一致の護石が既にある | 「同じ護石が登録済みです。同じ護石を複数所持している場合はそのまま登録できます」[登録する / やめる] |
| 4 | マスタ読込失敗 | 全画面表示(ContentUnavailableView) | 起動時 | bundled.db破損の通知 | 「データの読み込みに失敗しました。アプリの再インストールをお試しください」(操作不可) |
| 5 | 護石データ初期化通知 | alert | 起動時 | user.db破損からの復旧通知 | 「護石データを初期化しました」[OK] |

### 広告バナーの配置(2026-08-23決定)

- 表示画面: **検索条件・検索結果・護石一覧・情報**(各タブのルート+結果一覧)の最下部、タブバーの直上に固定
- 非表示画面: スキル選択・武器選択・護石入力(入力操作の誤タップ防止)、装備詳細・権利表記(情報の視認性優先)
- 読み込み失敗時: バナー領域ごと畳む(空白を残さない)

---

## 3. 画面遷移

```text
[起動]
  ├→ bundled.db読込失敗 → マスタ読込失敗(全画面・終端)
  └→ TabView
       ├→ [検索タブ] SearchCondition
       │     ├→ (sheet) SkillPicker ─選択→ 閉じて条件リストに反映
       │     ├→ (push) WeaponSelect ─選択/解除→ 戻る
       │     └→ [検索] → (push) SearchResults(引数: SearchCondition, Weapon?)
       │            ├→ 1件以上 → 行タップ → (push) EquipmentDetail(引数: EquipmentSet)
       │            └→ 0件 → 同画面内に逆引き結果を表示(遷移なし)
       │                   └→ 候補行タップ → (sheet) CharmEntry(引数: 提案護石のプリセット)※Phase 4
       ├→ [護石タブ] CharmList
       │     ├→ [追加] → (sheet) CharmEntry(引数: なし=新規)
       │     └→ 行タップ → (sheet) CharmEntry(引数: OwnedCharm=編集)
       └→ [情報タブ] Info
             └→ (push) Credits
```

### 遷移の規則

- タブごとに独立した `NavigationStack` を持つ。タブ切替で各スタックの状態は保持する
- push遷移は `NavigationStack` の `path`(`[Route]`)経由。Viewから直接 `NavigationLink(destination:)` で別Viewを生成しない(引数はRoute enumのassociated valueで渡す)
- 入力を伴う画面(スキル選択・護石入力)はsheet表示。sheetのスワイプ閉じは「キャンセル」と同義(護石入力で未保存変更があれば入力破棄確認を出す)
- 検索実行中に画面を離れた場合(タブ切替)は検索をキャンセルし、戻ったときは条件編集状態(仕様3.1)

---

## 4. 画面ごとの詳細

### 4.1 検索条件(Route: `searchCondition`)

#### 目的

欲しいスキルと目標レベルを組み、検索を実行する。アプリの起点となる画面。

#### 採用パターン

Data Entry(モバイル適用)— 条件リストを組み立てて実行する入力画面だから。

#### 領域構成

```text
┌──────────────────────────────┐
│ 検索条件            [リセット] │ ナビゲーションバー
├──────────────────────────────┤
│ 武器                          │ セクション
│  スラッシュアックス「暁光の…」 │ ← 未選択時「武器を選択(任意)」
├──────────────────────────────┤
│ スキル条件        [+ 追加]    │ セクション
│  攻撃         Lv3  [- +] ⊖   │ 行: スキル名+レベルStepper+削除
│  見切り       Lv2  [- +] ⊖   │
│  (空: 「スキルを追加して…」)  │
├──────────────────────────────┤
│        [ 検索する ]           │ 主ボタン(条件0件で無効)
├──────────────────────────────┤
│ [広告バナー]                  │
└──────────────────────────────┘
```

#### 構成要素

| # | 要素 | 種類 | ラベル/文言 | 必須 | 初期値 | 入力制約・振る舞い |
|---|---|---|---|---|---|---|
| 1 | リセット | ToolbarButton | リセット | - | - | 条件スキル全削除+武器選択解除。条件0件時は無効。確認なしで即実行(取り返しは再入力で軽微) |
| 2 | 武器行 | NavigationLink行 | 選択中の武器名(種別+名前)/未選択時「武器を選択(任意)」 | - | 前回選択(AppState.selectedWeaponId) | タップで `weaponSelect` へpush |
| 3 | スキル追加 | Button | + 追加 | - | - | `skillPicker` をsheet表示 |
| 4 | 条件スキル行 | List行 | スキル名+「Lv<n>」 | - | 追加時は最大レベル(2026-08-23決定) | Stepperで1〜maxLevelを増減。スワイプ削除可、⊖タップでも削除 |
| 5 | 検索する | Button(prominent) | 検索する | - | - | 条件スキル0件で無効。タップで `searchResults` へpush(検索は結果画面側で実行) |
| 6 | 広告バナー | AdBannerView | - | - | - | §2の配置規則に従う |

#### ViewModel

`app/MHSimulator/Sources/ViewModels/SearchConditionViewModel.swift`

| メンバー | 種別 | 型 | 役割 |
|---|---|---|---|
| `conditions` | プロパティ | `[ConditionRow]`(skillId・名前・level・maxLevel) | 条件スキルリストの表示元。追加順 |
| `selectedWeapon` | プロパティ | `Weapon?` | 選択中の武器。`nil` は未選択 |
| `canSearch` | 算出プロパティ | `Bool` | `!conditions.isEmpty` |
| `addSkill(_:)` | メソッド | `(Skill) -> Void` | スキル選択sheetからの追加。既存スキルなら行を最大レベルに更新(重複行を作らない=仕様3.1の後勝ち統合) |
| `removeSkill(_:)` | メソッド | `(SkillId) -> Void` | 条件行の削除 |
| `setLevel(_:_:)` | メソッド | `(SkillId, Int) -> Void` | Stepperの反映。1〜maxLevelにクランプ |
| `reset()` | メソッド | `() -> Void` | 条件全削除+武器解除 |
| `makeSearchCondition()` | メソッド | `() -> SearchCondition` | Core APIの `SearchCondition(requiredSkills:)` を構築 |

条件と武器選択は変更の都度 `AppState`(user.db)に保存し、次回起動時に復元する(仕様4.3 lastSearchConditions)。

#### 状態

| 状態 | 条件 | 表示 | 操作可否 |
|---|---|---|---|
| 初期 | 起動直後 | AppStateから前回条件・前回武器を復元して表示 | 全操作可 |
| 読み込み中 | (なし) | マスタは起動時に読込済みのため本画面に読み込み中はない | - |
| 正常 | 条件1件以上 | 条件リスト+有効な検索ボタン | 全操作可 |
| 空 | 条件0件 | スキル条件セクションに「スキルを追加して検索条件を作成してください」 | 検索・リセット無効。追加・武器選択は可 |
| エラー | (なし) | 本画面でのエラーはない(検索エラーは結果画面) | - |
| 実行中 | (なし) | 検索実行は結果画面側 | - |

#### バリデーション

| 対象 | 条件 | メッセージ | 表示位置 | 検証タイミング |
|---|---|---|---|---|
| 条件スキル | 0件 | (メッセージなし。検索ボタン無効+空状態文言で示す) | - | 常時 |
| レベル | 範囲外 | (発生しない。Stepperで構造的に防止) | - | - |

#### 遷移

- 入口: 起動直後(検索タブのルート)
- 出口: [検索する] → `searchResults`(引数: `SearchCondition` と `Weapon?`)/ 武器行 → `weaponSelect` / [+追加] → `skillPicker`(sheet)
- 離脱時: 条件は都度保存されるため確認なし

#### 規約から外れる点

- なし

#### 成果物

- `app/MHSimulator/Sources/Views/SearchConditionView.swift`
- `app/MHSimulator/Sources/ViewModels/SearchConditionViewModel.swift`

---

### 4.2 スキル選択(Route: `skillPicker`・sheet)

#### 目的

179スキルの中から条件に入れるスキルを、検索と分類で素早く見つけて選ぶ(仕様Q-13への回答)。

#### 採用パターン

Master Detail(一覧のみ)— 検索付き一覧から選ぶだけの画面だから。

#### 領域構成

```text
┌──────────────────────────────┐
│ キャンセル   スキルを選択      │ sheetヘッダ
│ [🔍 スキル名で検索]           │
│ [すべて|武器|防具|シリーズ|グループ] │ 分類フィルタ(Segmented)
├──────────────────────────────┤
│ 攻撃            武器  最大Lv5 │ 行: 名前+分類バッジ+最大Lv
│ 会心撃【属性】   武器  最大Lv3 │
│ …                            │
│ ✓ 見切り        武器  最大Lv7 │ ← 選択済みはチェック+薄色
└──────────────────────────────┘
```

#### 構成要素

| # | 要素 | 種類 | ラベル/文言 | 必須 | 初期値 | 入力制約・振る舞い |
|---|---|---|---|---|---|---|
| 1 | キャンセル | ToolbarButton | キャンセル | - | - | sheetを閉じる(選択の取り消しではない。追加済み分は残る) |
| 2 | 検索欄 | TextField(searchable) | プレースホルダ「スキル名で検索」 | - | 空 | 部分一致・入力の都度絞り込み。ひらがな/カタカナは同一視(かな正規化) |
| 3 | 分類フィルタ | Picker(segmented) | すべて/武器/防具/シリーズ/グループ | - | すべて | Skill.kindで絞り込み。シリーズ/グループスキルも同列に選べる(仕様Q-12: 同列と決定) |
| 4 | スキル行 | List行 | スキル名+分類バッジ+「最大Lv<n>」 | - | 名前昇順(五十音) | タップで条件に追加(最大レベル)しsheetは開いたまま(連続追加可)。選択済み行は✓表示、再タップで条件から削除 |

#### ViewModel

`app/MHSimulator/Sources/ViewModels/SkillPickerViewModel.swift`

| メンバー | 種別 | 型 | 役割 |
|---|---|---|---|
| `searchText` | プロパティ | `String` | 検索欄。変更の都度絞り込み再実行 |
| `kindFilter` | プロパティ | `SkillKindFilter`(all/weapon/armor/set/group) | 分類フィルタ |
| `visibleSkills` | 算出プロパティ | `[Skill]` | 絞り込み結果。名前昇順 |
| `selectedSkillIds` | プロパティ | `Set<SkillId>` | 条件に入っているスキル(親VMと共有) |
| `toggle(_:)` | メソッド | `(Skill) -> Void` | 追加/削除のトグル。親の `SearchConditionViewModel` に委譲 |

#### 状態

| 状態 | 条件 | 表示 | 操作可否 |
|---|---|---|---|
| 初期 | sheet表示直後 | 全179件を名前昇順で表示。検索欄フォーカスなし | 全操作可 |
| 読み込み中 | (なし) | メモリ上のマスタのため読み込みなし | - |
| 正常 | 絞り込み結果1件以上 | 一覧表示 | 全操作可 |
| 空(絞り込み0件) | 検索・フィルタ結果0件 | ContentUnavailableView「「<検索語>」に一致するスキルはありません」 | 検索欄・フィルタのみ可 |
| エラー | (なし) | - | - |
| 実行中 | (なし) | - | - |

#### バリデーション

なし(選択操作のみ)。

#### 遷移

- 入口: 検索条件画面の [+追加]
- 出口: [キャンセル] またはスワイプでsheetを閉じる
- 離脱時: 確認なし(追加は即時反映済み)

#### 規約から外れる点

- なし

#### 成果物

- `app/MHSimulator/Sources/Views/SkillPickerView.swift`
- `app/MHSimulator/Sources/ViewModels/SkillPickerViewModel.swift`

---

### 4.3 武器選択(Route: `weaponSelect`)

#### 目的

検索に反映する武器を1つ選ぶ、または選択を解除する(F-4)。

#### 採用パターン

Master Detail(一覧のみ)— 武器種→武器の2段の絞り込み一覧だから。

#### 領域構成

```text
┌──────────────────────────────┐
│ ← 検索条件      武器を選択     │
│ [🔍 武器名で検索]             │
│ [大剣 ▼] 武器種フィルタ        │
├──────────────────────────────┤
│ (選択解除)  武器を使わない     │ 先頭固定行
│ 暁光の…     R8  ①①─  攻撃+.. │ 行: 名前+レア+スロット+スキル
│ …                            │
└──────────────────────────────┘
```

#### 構成要素

| # | 要素 | 種類 | ラベル/文言 | 必須 | 初期値 | 入力制約・振る舞い |
|---|---|---|---|---|---|---|
| 1 | 検索欄 | TextField(searchable) | プレースホルダ「武器名で検索」 | - | 空 | 部分一致・入力の都度絞り込み |
| 2 | 武器種フィルタ | Picker(menu) | 14武器種+「すべて」 | - | 前回選択武器の種別、なければ「すべて」 | Weapon.kindで絞り込み |
| 3 | 選択解除行 | List行 | 武器を使わない | - | - | タップで選択解除し前画面へ戻る |
| 4 | 武器行 | List行 | 武器名+「R<rarity>」+スロット記号+武器スキル要約 | - | レア度降順→名前昇順 | タップで選択し前画面へ戻る。選択中の武器は✓表示 |

スロット記号の表記: サイズを①②③で並べる(例 スロット[3,1] → 「③①」)。空きなしは「─」。全画面共通(§6)。

#### ViewModel

`app/MHSimulator/Sources/ViewModels/WeaponSelectViewModel.swift`

| メンバー | 種別 | 型 | 役割 |
|---|---|---|---|
| `searchText` | プロパティ | `String` | 検索欄 |
| `kindFilter` | プロパティ | `String?` | 武器種(`nil`=すべて) |
| `visibleWeapons` | 算出プロパティ | `[Weapon]` | 絞り込み結果。レア度降順→名前昇順 |
| `selectedWeaponId` | プロパティ | `Int64?` | 選択中(親VMと共有) |
| `select(_:)` | メソッド | `(Weapon?) -> Void` | 選択/解除して戻る。AppStateに保存 |

#### 状態

| 状態 | 条件 | 表示 | 操作可否 |
|---|---|---|---|
| 初期 | 表示直後 | 前回の武器種フィルタを適用した一覧 | 全操作可 |
| 読み込み中 | (なし) | - | - |
| 正常 | 1件以上 | 一覧表示 | 全操作可 |
| 空(絞り込み0件) | 検索結果0件 | ContentUnavailableView「「<検索語>」に一致する武器はありません」 | 検索欄・フィルタのみ可 |
| エラー | (なし) | - | - |
| 実行中 | (なし) | - | - |

#### バリデーション

なし。

#### 遷移

- 入口: 検索条件画面の武器行
- 出口: 行タップで選択確定し `searchCondition` へ戻る(pop)
- 離脱時: 戻るボタンは選択変更なしで戻る

#### 規約から外れる点

- なし

#### 成果物

- `app/MHSimulator/Sources/Views/WeaponSelectView.swift`
- `app/MHSimulator/Sources/ViewModels/WeaponSelectViewModel.swift`

---

### 4.4 検索結果(Route: `searchResults`)

#### 目的

条件で組める装備一式を一覧で確認する。0件のときは逆引き(F-2)で「次の一手」を得る。

#### 採用パターン

パターン外 — 結果リストと逆引き提示が同居するため。領域構成図で補う。

#### 領域構成

```text
┌──────────────────────────────┐
│ ← 検索条件      検索結果       │
├──────────────────────────────┤
│ 100件(打ち切り) 防御力順      │ ヘッダ(件数+打ち切り注記)
│ ┌──────────────────────────┐ │
│ │ 防御力 512  空きスロ 武③ 防①① │ 結果カード
│ │ ゴアヘルムβ 他4部位        │ │
│ │ 攻撃Lv3 見切りLv2 …       │ │ 発動スキル要約
│ └──────────────────────────┘ │
│ …                            │
├──────────────────────────────┤
│ [広告バナー]                  │
└──────────────────────────────┘

--- 0件時(同画面内) ---
┌──────────────────────────────┐
│ 条件に合う装備は見つかりません  │
│ ▼ この護石があれば組めます     │ 逆引きセクション
│ ┌──────────────────────────┐ │
│ │ 攻撃Lv3 + 防具スロ①       │ │ 候補カード
│ │ レア8の護石で出現          │ │
│ └──────────────────────────┘ │
│ (または)                     │
│ ▼ このスキルを外せば組めます   │ 代替セクション
│  ・超会心                     │
└──────────────────────────────┘
```

#### 構成要素

| # | 要素 | 種類 | ラベル/文言 | 必須 | 初期値 | 入力制約・振る舞い |
|---|---|---|---|---|---|---|
| 1 | 件数ヘッダ | Text | 「<n>件」/打ち切り時「<n>件(上限で打ち切り。条件を絞ると精度が上がります)」 | - | - | SearchResult.truncatedで出し分け |
| 2 | 結果カード | List行(カード) | 防御力・空きスロット記号・部位代表名(頭部位名+「他4部位」)・発動スキル要約(条件スキル優先で最大4つ+「…」) | - | 防御力max降順 | タップで `equipmentDetail` へpush |
| 3 | 0件見出し | ContentUnavailableView | 「条件に合う装備は見つかりません」 | - | - | 0件時のみ |
| 4 | 逆引き候補カード | List行(カード) | 要求スキル(「攻撃Lv3」等)+要求スロット(「防具スロ①」等)+「レア<n>の護石で出現」 | - | レア度昇順(最大10件) | Phase 4以降: タップで護石入力sheetにプリセット表示。Phase 3ではタップ不可の表示のみ |
| 5 | 代替候補行 | List行 | 「<スキル名>を外すと組めます」 | - | - | CharmOracle.Outcome.relaxations時。タップで条件から該当スキルを外して自動再検索 |
| 6 | 逆引き不能文言 | Text | 「護石では埋まらない条件です。条件を見直してください」 | - | - | Outcome.none時 |
| 7 | 広告バナー | AdBannerView | - | - | - | §2の配置規則 |

#### ViewModel

`app/MHSimulator/Sources/ViewModels/SearchResultsViewModel.swift`

| メンバー | 種別 | 型 | 役割 |
|---|---|---|---|
| `phase` | プロパティ | `Phase`(searching / results(SearchResult) / reverseLookup(CharmOracle.Outcome) / failed(String)) | 画面の主状態 |
| `condition` | プロパティ | `SearchCondition` | 遷移引数 |
| `weapon` | プロパティ | `Weapon?` | 遷移引数 |
| `runSearch()` | メソッド | `async` | 表示時に実行。`SearchEngine.search` → 0件なら `CharmOracle.reverseLookup` を続けて実行(仕様3.1手順7)。バックグラウンドスレッドで実行し、キャンセルはTaskキャンセルで行う |
| `removeSkillAndRetry(_:)` | メソッド | `(SkillId) -> Void` | 代替候補タップ。条件から外して再検索(検索条件画面の条件も更新) |

`SearchEngine.Options` は `maxResults: 100`、`deadline: 5秒後`(仕様6.1の打ち切り予算)で呼ぶ。

#### 状態

| 状態 | 条件 | 表示 | 操作可否 |
|---|---|---|---|
| 初期 | push直後 | 即 `runSearch()` 実行→検索中へ | - |
| 検索中 | `phase == .searching` | ProgressView+「検索中…」。結果領域は空 | 戻るのみ可(戻ると検索キャンセル) |
| 正常 | 結果1件以上 | 件数ヘッダ+結果カードリスト | 全操作可 |
| 空(0件) | 結果0件 | 0件見出し+逆引きセクション(候補/代替/不能文言のいずれか)。逆引き計算中は「狙える護石を探しています…」+ProgressView | 逆引き候補・代替のタップ可 |
| エラー | `phase == .failed` | 「検索できませんでした。条件を減らしてお試しください」+[再試行] | 再試行・戻るのみ |
| 実行中 | (検索中と同一) | - | - |

#### バリデーション

なし(条件検証は検索条件画面とCore側 `SearchError` で担保。`SearchError` はこの画面ではエラー状態として表示)。

#### 遷移

- 入口: 検索条件画面の [検索する]
- 出口: 結果カード → `equipmentDetail`(引数: `EquipmentSet`)/ 逆引き候補 → `charmEntry`(sheet・プリセット引数。Phase 4)
- 離脱時: 検索・逆引きのTaskをキャンセル。途中結果は破棄(仕様3.1)

#### 規約から外れる点

- なし

#### 成果物

- `app/MHSimulator/Sources/Views/SearchResultsView.swift`
- `app/MHSimulator/Sources/ViewModels/SearchResultsViewModel.swift`

---

### 4.5 装備詳細(Route: `equipmentDetail`)

#### 目的

検索結果1件の内訳(部位・護石・装飾品・発動スキル・防御/耐性・空きスロット)を確認する(F-6)。

#### 採用パターン

パターン外 — 静的な詳細カードの縦積み。領域構成図で補う。

#### 領域構成

```text
┌──────────────────────────────┐
│ ← 検索結果      装備詳細       │
├──────────────────────────────┤
│ 防御力 512   耐性 🔥-2 💧1 ⚡0 ❄1 🐉3 │ サマリ
├──────────────────────────────┤
│ 装備                          │
│  武器  暁光の断剣 ③①         │ (未選択時は行ごと非表示)
│  頭    ゴアヘルムβ ①         │
│  胴/腕/腰/脚 …                │
│  護石  攻撃の護石Ⅲ / 所持護石  │ (護石なし時「護石: なし」)
├──────────────────────────────┤
│ 装飾品                        │
│  痛撃珠【3】 → 頭 スロ③       │ 割り当て先付き
│  (なし時「装飾品: なし」)     │
├──────────────────────────────┤
│ 発動スキル                    │
│  攻撃 Lv3 ★条件               │ 条件スキルに★
│  見切り Lv2 ★条件             │
│  ひるみ軽減 Lv1               │
├──────────────────────────────┤
│ 空きスロット 武器─ / 防具②①   │
└──────────────────────────────┘
```

#### 構成要素

| # | 要素 | 種類 | ラベル/文言 | 必須 | 初期値 | 入力制約・振る舞い |
|---|---|---|---|---|---|---|
| 1 | サマリ | カード | 「防御力 <n>」+5耐性(火水雷氷龍の順・アイコン+数値) | - | - | EquipmentSet.totalDefenseMax / totalResistances |
| 2 | 装備リスト | List(Section) | 武器/頭/胴/腕/腰/脚/護石の各行: 名前+スロット記号 | - | - | 表示のみ。タップ動作なし(MVP) |
| 3 | 装飾品リスト | List(Section) | 「<装飾品名> → <装着部位> スロ<サイズ>」 | - | - | DecorationAssignment のowner/slotSizeから表示 |
| 4 | 発動スキルリスト | List(Section) | 「<スキル名> Lv<n>」+条件スキルに「★条件」バッジ | - | 条件スキル→レベル降順→名前順 | activeSkills から。シリーズ/グループスキルも同列表示 |
| 5 | 空きスロット | Text | 「武器<記号> / 防具<記号>」 | - | - | emptyWeaponSlots / emptyArmorSlots |

#### ViewModel

`app/MHSimulator/Sources/ViewModels/EquipmentDetailViewModel.swift`

| メンバー | 種別 | 型 | 役割 |
|---|---|---|---|
| `set` | プロパティ | `EquipmentSet` | 遷移引数。表示専用 |
| `condition` | プロパティ | `SearchCondition` | ★条件バッジの判定用 |
| `skillName(_:)` | メソッド | `(SkillId) -> String` | MasterDatabase.skillsから名前解決 |

#### 状態

| 状態 | 条件 | 表示 | 操作可否 |
|---|---|---|---|
| 初期/正常 | 常時 | 引数のEquipmentSetを表示(読み込みなし) | 戻るのみ |
| 読み込み中/空/エラー/実行中 | (なし) | 発生しない(引数が常に完全なデータ) | - |

#### バリデーション

なし。

#### 遷移

- 入口: 検索結果の行タップ
- 出口: 戻るのみ
- 離脱時: 確認なし

#### 規約から外れる点

- なし

#### 成果物

- `app/MHSimulator/Sources/Views/EquipmentDetailView.swift`
- `app/MHSimulator/Sources/ViewModels/EquipmentDetailViewModel.swift`

---

### 4.6 護石一覧(Route: `charmList`)

#### 目的

所持している鑑定護石を確認し、追加・編集・削除する(F-3)。

#### 採用パターン

Master Detail(一覧のみ)— 一覧と編集(sheet)の組み合わせだから。

#### 領域構成

```text
┌──────────────────────────────┐
│ 護石                 [+ 追加] │
├──────────────────────────────┤
│ 攻撃Lv2・見切りLv1  R7 ②─    │ 行: スキル+レア+スロット
│ 匠Lv2              R6 ①①    │
│ …                            │
│ <n>個                        │
├──────────────────────────────┤
│ [広告バナー]                  │
└──────────────────────────────┘
```

#### 構成要素

| # | 要素 | 種類 | ラベル/文言 | 必須 | 初期値 | 入力制約・振る舞い |
|---|---|---|---|---|---|---|
| 1 | 追加 | ToolbarButton | + 追加 | - | - | `charmEntry` をsheet表示(新規) |
| 2 | 護石行 | List行 | スキルを「・」区切り(例「攻撃Lv2・見切りLv1」)+「R<rarity>」+スロット記号(武器スロは「武①」表記) | - | 登録日時降順 | タップで `charmEntry`(編集)。スワイプで削除(護石削除確認ダイアログ経由) |
| 3 | 件数表示 | Text | 「<n>個」 | - | - | リスト末尾 |
| 4 | 広告バナー | AdBannerView | - | - | - | §2の配置規則 |

#### ViewModel

`app/MHSimulator/Sources/ViewModels/CharmListViewModel.swift`

| メンバー | 種別 | 型 | 役割 |
|---|---|---|---|
| `charms` | プロパティ | `[OwnedCharmItem]`(id・表示文字列・Charm) | 一覧の表示元。user.dbから読込 |
| `isLoading` | プロパティ | `Bool` | 初回読み込み中 |
| `loadErrorMessage` | プロパティ | `String?` | 読み込み失敗文言 |
| `load()` | メソッド | `async` | 一覧再読込。表示時と保存後に実行 |
| `delete(_:)` | メソッド | `(UUID) async -> Void` | 削除(確認ダイアログ通過後)。1トランザクション。失敗時はalert「削除できませんでした。端末の空き容量をご確認ください」 |

#### 状態

| 状態 | 条件 | 表示 | 操作可否 |
|---|---|---|---|
| 初期 | 表示直後 | `load()` 実行→読み込み中へ | - |
| 読み込み中 | `isLoading` | ProgressView(1,000件でも1秒以内=仕様6.1のため一瞬) | 操作不可 |
| 正常 | 1件以上 | 一覧+件数 | 全操作可 |
| 空(0件) | 読み込み成功かつ0件 | ContentUnavailableView「護石がまだ登録されていません」+「[+追加] から所持している鑑定護石を登録してください」+[追加する]ボタン | 追加のみ可 |
| エラー | `loadErrorMessage != nil` | 文言+[再試行] | 再試行のみ可 |
| 実行中(削除) | 削除処理中 | 対象行をディム表示 | 他行の操作・追加は不可(削除連打防止=仕様3.3) |

#### バリデーション

なし(一覧のみ)。

#### 遷移

- 入口: 護石タブ
- 出口: [+追加]/行タップ → `charmEntry`(sheet)
- 離脱時: 確認なし

#### 規約から外れる点

- なし

#### 成果物

- `app/MHSimulator/Sources/Views/CharmListView.swift`
- `app/MHSimulator/Sources/ViewModels/CharmListViewModel.swift`

---

### 4.7 護石入力(Route: `charmEntry`・sheet)

#### 目的

抽選規則の入力補助で、あり得る鑑定護石だけを速く正確に登録・編集する(F-3の中核)。

#### 採用パターン

Data Entry — 段階的に確定していく入力フォームだから。

#### 領域構成

```text
┌──────────────────────────────┐
│ キャンセル  護石を登録   [保存] │
├──────────────────────────────┤
│ スキル                        │
│  スキル1  [攻撃 Lv2      ▼]  │ タップで候補Picker(sheet内push)
│  スキル2  [見切り Lv1    ▼]  │ スキル1確定後に有効化
│  スキル3  [(なし)        ▼]  │ スキル2確定後に有効化
├──────────────────────────────┤
│ スロット・レア度               │
│  ( ) R7  ②─                  │ 規則から自動候補(単一選択)
│  (o) R7  ①①                  │ 候補1つなら自動選択
├──────────────────────────────┤
│ メモ [_________________]      │ 任意
└──────────────────────────────┘
```

#### 構成要素

| # | 要素 | 種類 | ラベル/文言 | 必須 | 初期値 | 入力制約・振る舞い |
|---|---|---|---|---|---|---|
| 1 | キャンセル | ToolbarButton | キャンセル | - | - | 変更ありなら入力破棄確認ダイアログ。なければ即閉じ |
| 2 | 保存 | ToolbarButton | 保存 | - | - | 無効条件: スキル1未選択 or スロット候補未選択 or 保存中。完全一致の登録済み護石があれば重複登録警告(alert)を経て保存 |
| 3 | スキル1 | 行+候補Picker | 「スキル1」/未選択時「スキルを選択」 | 必須 | 編集時: 既存値 | 候補 = `CharmRules.candidates(forPosition: 0, previous: [])`。検索欄付きリスト(スキル名+Lv)。選択するとスキル2・3の選択済み値がルール上あり得なければクリアする |
| 4 | スキル2 | 行+候補Picker | 「スキル2」/「(なし)」選択可 | - | 編集時: 既存値 | スキル1未確定時は無効(グレー表示)。候補 = `candidates(forPosition: 1, previous: [スキル1])` +先頭に「(なし)」。「(なし)」選択でスキル3もクリアして無効化 |
| 5 | スキル3 | 行+候補Picker | 「スキル3」/「(なし)」選択可 | - | 編集時: 既存値 | スキル2確定後に有効。候補 = `candidates(forPosition: 2, previous: [スキル1, スキル2])` +「(なし)」 |
| 6 | スロット・レア度候補 | 単一選択リスト | 「R<rarity>  <スロット記号>」(武器スロは「武①」) | 必須 | 候補が1つなら自動選択 | 候補 = `CharmRules.slotCandidates(for:)`。スキル構成変更の都度100ms以内に再計算(仕様6.1)。選択済み候補が消えた場合は未選択に戻す |
| 7 | メモ | TextField | プレースホルダ「メモ(任意)」 | - | 編集時: 既存値 | 100文字まで |

#### ViewModel

`app/MHSimulator/Sources/ViewModels/CharmEntryViewModel.swift`

| メンバー | 種別 | 型 | 役割 |
|---|---|---|---|
| `editingCharmId` | プロパティ | `UUID?` | `nil`=新規、非nil=編集 |
| `skill1` / `skill2` / `skill3` | プロパティ | `CharmRules.GroupEntry?` | 選択中スキル(スキル+レベルの組) |
| `skill1Candidates` | 算出プロパティ | `[CharmRules.GroupEntry]` | 位置0の候補。名前昇順→レベル昇順 |
| `skill2Candidates` / `skill3Candidates` | 算出プロパティ | 同上 | 位置1/2の候補(前段選択に追従) |
| `slotCandidates` | 算出プロパティ | `[CharmRules.RaritySlots]` | レア度昇順→スロット合計降順 |
| `selectedSlot` | プロパティ | `CharmRules.RaritySlots?` | 選択中のスロット・レア度 |
| `memo` | プロパティ | `String` | メモ |
| `isDirty` | プロパティ | `Bool` | 初期値からの変更有無 |
| `isSaving` | プロパティ | `Bool` | 保存中(二度押し防止) |
| `canSave` | 算出プロパティ | `Bool` | `skill1 != nil && selectedSlot != nil && !isSaving` |
| `save()` | メソッド | `async throws` | user.dbに1トランザクションで保存(仕様3.3)。重複チェック→警告→保存。失敗時は入力保持のままエラー表示 |

編集時、既存データが現規則で候補に存在しない場合(旧規則で登録済み)はそのまま表示し、変更した時点から規則補助が効く(仕様3.3エッジケース)。

#### 状態

| 状態 | 条件 | 表示 | 操作可否 |
|---|---|---|---|
| 初期(新規) | 引数なし | 全項目未選択。スキル2・3とスロット候補は無効 | スキル1・キャンセルのみ実質有効 |
| 初期(編集) | OwnedCharm引数 | 既存値を全項目に反映 | 全操作可 |
| 初期(プリセット) | 逆引き候補引数 | 要求スキル・スロットを初期選択(Phase 4) | 全操作可 |
| 正常 | 入力途中 | 選択状況に応じ段階的に有効化 | 上記の各行規則どおり |
| 空(候補0件) | (構造上発生しない: 規則上あり得ない組み合わせは選択肢に出ない) | - | - |
| エラー | 保存失敗 | フォーム下部に「保存できませんでした。端末の空き容量をご確認ください」 | 入力保持のまま再操作可 |
| 実行中 | `isSaving` | 保存ボタンをProgressViewに差し替え | 全入力とキャンセル不可 |
| 規則データなし | `CharmRules.isEmpty` | フォーム全体を無効化し「護石データを更新してください」 | キャンセルのみ(仕様5.2) |

#### バリデーション

| 対象 | 条件 | メッセージ | 表示位置 | 検証タイミング |
|---|---|---|---|---|
| スキル1 | 未選択 | (メッセージなし。保存無効で示す) | - | 常時 |
| スロット候補 | 未選択 | (同上) | - | 常時 |
| メモ | 100文字超 | (入力を100文字で打ち切り。メッセージなし) | - | 入力の都度 |
| 重複 | 完全一致が登録済み | 重複登録警告ダイアログ(§2の文言) | alert | 保存実行時 |

あり得ない組み合わせはUIに出ないため入力エラーは構造的に発生しない(仕様3.3)。

#### 遷移

- 入口: 護石一覧 [+追加]/行タップ、検索結果の逆引き候補(Phase 4)
- 出口: [保存] → 閉じて護石一覧を再読込 / [キャンセル] → 破棄確認(変更ありのとき)→ 閉じる
- 離脱時: sheetスワイプはキャンセルと同じ扱い(`interactiveDismissDisabled` を `isDirty` 時に有効化し、破棄確認を必ず経由させる)

#### 規約から外れる点

- なし

#### 成果物

- `app/MHSimulator/Sources/Views/CharmEntryView.swift`
- `app/MHSimulator/Sources/ViewModels/CharmEntryViewModel.swift`

---

### 4.8 情報(Route: `info`)

#### 目的

アプリ情報・データバージョン・権利表記への入口を提供する。

#### 採用パターン

Settings App(簡易)— 項目の少ない設定型リストだから。

#### 領域構成

```text
┌──────────────────────────────┐
│ 情報                          │
├──────────────────────────────┤
│ このアプリについて             │
│  バージョン        1.0.0 (1)  │
│  ゲームデータ      2026-04時点 │
│  護石規則データ    2026-08-22  │
├──────────────────────────────┤
│ 権利表記・クレジット      >    │ → credits へ
├──────────────────────────────┤
│ 本アプリは非公式のファンメイド  │ フッタ注記
│ アプリです                    │
├──────────────────────────────┤
│ [広告バナー]                  │
└──────────────────────────────┘
```

#### 構成要素

| # | 要素 | 種類 | ラベル/文言 | 必須 | 初期値 | 入力制約・振る舞い |
|---|---|---|---|---|---|---|
| 1 | バージョン行 | LabeledContent | 「バージョン」+CFBundleShortVersionString (build) | - | - | 表示のみ |
| 2 | ゲームデータ行 | LabeledContent | 「ゲームデータ」+Meta.sourceCommitの取得時期表記 | - | - | 表示のみ |
| 3 | 規則データ行 | LabeledContent | 「護石規則データ」+Meta.charmRulesVersion | - | - | 表示のみ |
| 4 | 権利表記リンク | NavigationLink | 権利表記・クレジット | - | - | `credits` へpush |
| 5 | 非公式注記 | Text(footer) | 「本アプリは非公式のファンメイドアプリです」 | - | - | セクションフッタ |
| 6 | 広告バナー | AdBannerView | - | - | - | §2の配置規則 |

#### ViewModel

`app/MHSimulator/Sources/ViewModels/InfoViewModel.swift`

| メンバー | 種別 | 型 | 役割 |
|---|---|---|---|
| `appVersion` | プロパティ | `String` | Bundleから取得 |
| `dataVersion` | プロパティ | `String` | MasterDatabase.charmRulesVersion 等の表示文字列 |

#### 状態

| 状態 | 条件 | 表示 | 操作可否 |
|---|---|---|---|
| 初期/正常 | 常時 | 全行表示(読み込みなし) | 全操作可 |
| その他 | (なし) | 発生しない | - |

#### バリデーション

なし。

#### 遷移

- 入口: 情報タブ
- 出口: 権利表記リンク → `credits`
- 離脱時: 確認なし

#### 規約から外れる点

- なし

#### 成果物

- `app/MHSimulator/Sources/Views/InfoView.swift`
- `app/MHSimulator/Sources/ViewModels/InfoViewModel.swift`

---

### 4.9 権利表記(Route: `credits`)

#### 目的

著作権の帰属・データ出典・非公式であることを明示する(2026-08-23決定の権利表記画面)。

#### 採用パターン

パターン外 — スクロールする静的文書。

#### 領域構成

```text
┌──────────────────────────────┐
│ ← 情報       権利表記          │
├──────────────────────────────┤
│ (ScrollView・静的テキスト)     │
│  著作権について                │
│  データ出典                    │
│  抽選規則データについて         │
│  免責                         │
└──────────────────────────────┘
```

#### 構成要素

| # | 要素 | 種類 | ラベル/文言 | 必須 | 初期値 | 入力制約・振る舞い |
|---|---|---|---|---|---|---|
| 1 | 本文 | Text(ScrollView) | 下記の確定文言 | - | - | 表示のみ。リンクはたどれる(Safari) |

確定文言(2026-08-23決定のクレジット文面+補足):

> **著作権について**
> 本アプリは非公式のファンメイドアプリです。株式会社カプコンおよび関連会社とは一切関係ありません。
> ゲームデータ・アイコン等のゲーム由来コンテンツの著作権は株式会社カプコンに帰属します。
> 権利者からの申し立てがあった場合、本アプリは速やかに公開を停止します。
>
> **データ出典**
> データ: mhdb-wilds-data (LartTyler) を加工して使用
> https://github.com/LartTyler/mhdb-wilds-data
>
> **鑑定護石の抽選規則データについて**
> 出現パターンはコミュニティの解析情報をもとに構成しています。実際のゲーム内容と異なる場合があります。
>
> **免責**
> 本アプリの利用により生じたいかなる損害についても、開発者は責任を負いません。

#### ViewModel

なし(静的画面のためViewのみ。`CreditsView` に文言を直接持つ)。

#### 状態

| 状態 | 条件 | 表示 | 操作可否 |
|---|---|---|---|
| 初期/正常 | 常時 | 静的表示 | 戻るのみ |

#### バリデーション

なし。

#### 遷移

- 入口: 情報画面
- 出口: 戻るのみ
- 離脱時: 確認なし

#### 規約から外れる点

- ViewModelを持たない(静的文書のため。MVVM規約の例外として明記)

#### 成果物

- `app/MHSimulator/Sources/Views/CreditsView.swift`

---

## 5. 共通UI規約への参照

| 事項 | 参照先 |
|---|---|
| 余白・間隔 | SwiftUI標準(List/Form/Sectionの既定) |
| フォント | システムフォント+Dynamic Type対応(仕様6.4) |
| 色 | システムカラー(ライト/ダーク両対応)。独自色は定義しない |
| コアAPI | `app/MHSimulatorCore/`(SearchEngine / CharmOracle / CharmRules / MasterDatabase / ResultValidator) |
| 永続化 | 仕様.md 5章(bundled.db読み取り専用 / user.db WAL・1操作1トランザクション) |

## 6. 全画面共通の振る舞い

| 事項 | 決定 |
|---|---|
| 起動時読み込み | 起動直後にbundled.dbを読み込む(仕様5.2)。読込中はスプラッシュ相当のProgressView。失敗時はダイアログ4(全画面・終端) |
| スロット記号 | サイズを①②③で表記し大きい順に並べる(埋め草はしない)。武器/防具を区別して並べる場面では「武」「防」プレフィックスを付ける(例「武③ 防①①」)。空きなし・該当なしは「─」 |
| スキル表記 | 「<スキル名>Lv<n>」。一覧での複数連結は「・」区切り |
| レア度表記 | 「R<n>」 |
| 完了通知 | トーストは使わない。保存・削除は画面遷移とリスト反映で伝える |
| 破壊的操作の確認 | confirmationDialog(destructiveロールのボタン) |
| 想定外の例外 | 画面内エラー表示+[再試行]に落とし、操作可能な状態に戻す。クラッシュレポータは入れない(仕様6.4) |
| 空状態の型 | ContentUnavailableView:「<対象>がまだありません/見つかりません」+次の行動を促す1文+主アクション |
| 広告 | AdBannerView(共通部品)。§2の配置規則。SDKは未確定事項1 |
| 回転 | 縦固定(仕様6.2) |

## 7. 実装タスク

Phase番号は企画書ロードマップ(Phase 3: 最小UI / Phase 4: 護石管理と逆引きUI / Phase 5: 磨き)に合わせる。

## 3-1. Xcodeプロジェクト作成と起動基盤

### タスク

Xcodeプロジェクトを作成し、MHSimulatorCoreをローカルパッケージ参照。bundled.dbをバンドルに同梱し、起動時読み込み(仕様5.2)とTabView骨格を作る。

### 含めるUI

- TabView(検索/護石/情報。護石・情報はプレースホルダ)
- 起動時ProgressViewとマスタ読込失敗表示(ダイアログ4)

### 依存

なし(MHSimulatorCoreは実装済み)

### 成果物

app/MHSimulator.xcodeproj
app/MHSimulator/Sources/MHSimulatorApp.swift
app/MHSimulator/Sources/AppDependencies.swift

### 優先度

MVP必須

## 3-2. 検索条件画面+スキル選択

### タスク

検索条件の組み立てとスキル選択sheet。条件のAppState永続化はPhase 4に送り、まずメモリ保持で動かす。

### 含めるUI

- 4.1の構成要素(広告バナー除く)
- 4.2の全構成要素

### 依存

3-1

### 成果物

app/MHSimulator/Sources/Views/SearchConditionView.swift
app/MHSimulator/Sources/ViewModels/SearchConditionViewModel.swift
app/MHSimulator/Sources/Views/SkillPickerView.swift
app/MHSimulator/Sources/ViewModels/SkillPickerViewModel.swift

### 優先度

MVP必須

## 3-3. 検索結果+装備詳細

### タスク

検索実行(バックグラウンド+キャンセル対応)と結果一覧・装備詳細。0件時はまず「0件+逆引きは準備中」の表示までを実装し、逆引き表示は3-4で入れる。

### 含めるUI

- 4.4の構成要素1〜3(広告バナー除く)
- 4.5の全構成要素

### 依存

3-2

### 成果物

app/MHSimulator/Sources/Views/SearchResultsView.swift
app/MHSimulator/Sources/ViewModels/SearchResultsViewModel.swift
app/MHSimulator/Sources/Views/EquipmentDetailView.swift
app/MHSimulator/Sources/ViewModels/EquipmentDetailViewModel.swift

### 優先度

MVP必須

## 3-4. 逆引き表示(閲覧のみ)

### タスク

0件時のCharmOracle実行と候補・代替・不能文言の表示(4.4の構成要素4〜6)。候補タップからの護石登録連携はPhase 4。

### 依存

3-3

### 成果物

(3-3のView/ViewModelに追加)

### 優先度

MVP必須(中核価値のため)

## 4-1. user.db基盤と護石一覧

### タスク

user.dbの作成・WAL・integrity_check・スキーマ(仕様4.3/5章)。SQLiteアクセス方式(Q-5: GRDB仮決定)をここで確定。護石一覧画面とAppState永続化(検索条件・武器)。

### 含めるUI

- 4.6の全構成要素(広告バナー除く)+ダイアログ1・5

### 依存

3-1

### 成果物

app/MHSimulator/Sources/Data/UserStore.swift
app/MHSimulator/Sources/Views/CharmListView.swift
app/MHSimulator/Sources/ViewModels/CharmListViewModel.swift

### 優先度

MVP必須

## 4-2. 護石入力(規則ベース補助)

### タスク

CharmRulesの候補APIを使った段階的入力補助と保存。ダイアログ2・3。

### 含めるUI

- 4.7の全構成要素

### 依存

4-1

### 成果物

app/MHSimulator/Sources/Views/CharmEntryView.swift
app/MHSimulator/Sources/ViewModels/CharmEntryViewModel.swift

### 優先度

MVP必須

## 4-3. 武器選択と逆引き→登録連携

### タスク

武器選択画面(F-4)と、逆引き候補タップ→護石入力プリセットの連携。

### 含めるUI

- 4.3の全構成要素
- 4.4の候補カードタップ動作

### 依存

3-4、4-2

### 成果物

app/MHSimulator/Sources/Views/WeaponSelectView.swift
app/MHSimulator/Sources/ViewModels/WeaponSelectViewModel.swift

### 優先度

MVP必須

## 5-1. 情報・権利表記・広告

### タスク

情報タブ・権利表記画面・広告バナー(SDK導入と共通AdBannerView)。

### 含めるUI

- 4.8・4.9の全構成要素
- AdBannerView(検索条件・検索結果・護石一覧・情報)

### 依存

3-1(広告は全画面に関わるため最後にまとめて入れる)

### 成果物

app/MHSimulator/Sources/Views/InfoView.swift
app/MHSimulator/Sources/ViewModels/InfoViewModel.swift
app/MHSimulator/Sources/Views/CreditsView.swift
app/MHSimulator/Sources/Views/AdBannerView.swift

### 優先度

MVP必須(Store公開前)

## 5-2. アイコン組み込み・磨き

### タスク

抽出アイコン(スキル系統・装飾品色・武器種)のマッピング表示、視認性調整、性能実機検証。

### 依存

3-3、4-2

### 成果物

app/MHSimulator/Assets.xcassets(アイコンセット)

### 優先度

MVPでは後回し可(アイコンなしでもテキストで成立する設計)

## 8. 未確定事項

今後決めるべき点。実装前にユーザーへ確認する。

1. **広告SDKの選定** — AdMobを想定してよいか。バナーのみか、将来インタースティシャルも視野に入れるか(配置設計に影響)
2. **抽出アイコンの入手方法と時期** — PAK展開(企画書Phase 7)を前倒すか、当面テキスト表示(5-2を後回し)でStore公開まで行くか
3. **App Storeプライバシー申告** — 広告SDK導入に伴うトラッキング許可(ATT)ダイアログを出すか(SDK選定に依存)
4. **検索条件の複数保持(マイセット)** — MVP外と決定済みだが、AppStateに「最後の1件」だけ保存する現設計で第一ユーザー検証に耐えるか
5. **護石一覧の並び替え・絞り込み** — 登録日時降順固定で開始し、実使用で不便なら追加(現状は仕様外)
